yarl==0.18.0
aiohttp>=2.0.0,<2.3.0
appdirs==1.4.3
raven==6.5.0
colorama==0.3.9
aiohttp-json-rpc==0.8.7
pyyaml==3.12
fuzzywuzzy[speedup]<=0.16.0
Red-Trivia>=1.1.1
async-timeout<3.0.0
# If accepted, the below should be modified to download a tagged version
# with tags matching the corresponding red version.
# We should NOT put this particular one on pypi due to complications with
# relicensing and pypi
git+https://github.com/Cog-Creators/discord.py/archive/rewrite.zip